<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Settings</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
            font-family: Arial, sans-serif;
        }
        .container {
            background-color: white;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>
<div class="container">
    <h1>Settings</h1>
    <form action="/settings" method="POST">
        <label>
            <input type="checkbox" name="useDefault" <%= useDefault ? 'checked' : '' %>> 使用默认问题
        </label><br><br>
        <label for="api-key">API Key:</label><br>
        <input type="text" name="apiKey" id="api-key" value="<%= apiKey %>"><br><br>
        <button type="submit">提交</button>
    </form>
</div>
</body>
</html>
